class UIScene extends Phaser.Scene {
    constructor(){
        super({
            key:'UIScene',
        });
    }

    create(){
        
        // var gameScene = this.scene.get('GameScene');
        // new Button(gameScene,100,100,'button')
        // var ui=this.add.sprite(0, 0, 'ui').setScale(5);
        // Phaser.Display.Align.In.BottomCenter(ui, gameScene.bg);
    }
}